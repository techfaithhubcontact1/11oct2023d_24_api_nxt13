//1. Import Area 

//2. Defone Ara
export async function GET () {
   // Every funtion may return somrthing 
   // the main objective of GET function is return something
   //Object.method(AtualArguments)
   return Response.json( {"keys":'Values'} )

}


//3. Export Area